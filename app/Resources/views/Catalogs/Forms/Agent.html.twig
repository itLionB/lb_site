{% extends 'base.html.twig' %}
{% block title %}Edit Info{% endblock %}
{% block body %}

<nav class="navbar navbar-inverse">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('home') }}">Lion Buildings</a>
      <ul class="nav navbar-nav">
        <li><a href="{{ path('catalog') }}">Catalogs</a></li>
        <li class="active"><a href="{{ path('installation') }}">Installation</a></li>
        <li><a href="{{ path('show_dashboard') }}">Dashboard</a></li>
      </ul>
      <a class="btn btn-default navbar-btn pull-right" href="{{ logout_path('main') }}">close sesion</a>  
    </div>
  </nav>

    <div class ="container">
        <br><a href="{{ path('show_agent') }}" class="btn btn-info">Back</a><br><br>
        <div class="panel panel-primary ">
            <div class="panel-heading ">
                <h3 class="panel-title">Edit Form</h3>
            </div>
            <div class="panel-body">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                {{ form_end(form) }}
                <br>
            </div>
        </div> 
    </div>


{% endblock %}