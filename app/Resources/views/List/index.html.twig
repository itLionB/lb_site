{% extends 'base.html.twig' %}

{% block body %}

<nav class="navbar navbar-inverse">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('home') }}">Lion Buildings</a>
      <ul class="nav navbar-nav">
        <li><a href="{{ path('catalog') }}">Catalogs</a></li>
        <li class=""><a href="{{ path('installation') }}">Installation</a></li>
        <li><a href="{{ path('list') }}">Lists</a></li>
      </ul>
      <a class="btn btn-default navbar-btn pull-right" href="{{ logout_path('main') }}">close sesion</a>  
    </div>
  </nav>


  <div class="container">
    <div class="panel panel-primary">
      <div class="panel-heading">
        <h3 class="panel-title">Choose one list</h3>
      </div>
      <div class="panel-body">
        <center>
          <a class="btn btn-info btn-block btn-catalog shadow" href="{{ path('hot_pending') }}">Hot Pending</a>
          <a class="btn btn-info btn-block btn-catalog shadow" href="{{ path('installation') }}">Installations</a>
          <a class="btn btn-info btn-block btn-catalog shadow" href="{{ path('show_manufacturer') }}">Installed</a>
          <a class="btn btn-info btn-block btn-catalog shadow" href="{{ path('show_agent') }}">Notifications</a>
        </center>
      </div>
    </div>

{% endblock %}